{
  "name": "E-S",
  "version": "1.0.1",
  "description": "to run backend `cd server && node app.js` to run frontend `cd client && npm run start`",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "start": "npm run server & npm run client"
  },
  "keywords": [
    "EV Charging",
    "Electric Vehicle"
  ],
  "author": "Falcon",
  "license": "ISC",
  "dependencies": {
    "@google/maps": "^0.5.5"
  },
  "devDependencies": {
    "eslint-plugin-react": "^7.14.2"
  }
}
